{
  "collectionName": "components_global_wallets",
  "info": {
    "name": "Wallet",
    "icon": "briefcase",
    "description": ""
  },
  "options": {},
  "attributes": {
    "address": {
      "type": "string",
      "required": true
    },
    "network": {
      "type": "enumeration",
      "enum": [
        "Ethereum",
        "Enjin",
        "BSC",
        "Solana"
      ],
      "default": "Ethereum",
      "required": true
    },
    "private_key": {
      "type": "text",
      "private": true
    },
    "connection": {
      "type": "enumeration",
      "enum": [
        "Metamask",
        "Automatic"
      ],
      "default": "Automatic",
      "required": true
    }
  }
}
